# astra-notebook — ASTRA/Generator 前后处理工作台依赖
# 安装: pip install -r requirements.txt
# 说明: 本项目不做发布包, 复制整个文件夹即可使用。
#       ASTRA/Generator 可执行文件需另行放置 (见 README.md)。

# ---- 核心科学计算 ----
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# ---- 数据导出 (CSV 表格) ----
pandas>=2.0

# ---- Jupyter 前端 ----
jupyter>=1.0
ipywidgets>=8.0

# ---- Notebook 执行/格式 (第 5 层 e2e 测试需要) ----
nbconvert>=7.0
nbformat>=5.0

# ---- 测试 (可选) ----
pytest>=7.0

# 注: PyCharm 打开项目后会自动检测本文件并提示一键安装;
#     若用项目自带的 .venv 则无需安装 (已全部装好)。
#     pyflakes 为开发期静态检查工具 (可选): pip install pyflakes

# ---- 可选: 交互式绘图 (锦上添花, 默认不开) ----
# ipympl>=0.9

# 测试与质量
pytest>=7.0
pytest-cov>=4.0
ruff>=0.1.0
pyflakes>=3.0

# Notebook 执行测试 (第 5 层) — 已由 requirements.txt 覆盖

